# Required for every live call. Create a key at https://console.typesafe.ai/keys
TYPESAFE_API_KEY=
# Optional. Defaults to jev-latest; pin a version once thresholds are tuned.
TYPESAFE_DEFAULT_MODEL=jev-latest
# Optional. Defaults to https://api.typesafe.ai
TYPESAFE_BASE_URL=
