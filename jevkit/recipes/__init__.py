"""One module per pattern. Import the module you need directly.

Each module is self-contained on purpose: its questions and thresholds live at
the top of its own file so a reviewer reads one screen, not a shared registry.
"""
